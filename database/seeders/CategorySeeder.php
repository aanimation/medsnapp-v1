<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
// use App\Models\Category;

class CategorySeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        /*
        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Streak';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Rank';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Levels';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Investigations';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Treatments';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Points';
        $item->save();

        $item = New Category();
        $item->category_type = 'badge';
        $item->category_name = 'Coins';
        $item->save();

        $item = New Category();
        $item->category_name = 'Default';
        $item->category_type = 'user';
        $item->save();
        */
    }
}
